<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>Connexion - Bekri</title>
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <style>
        /* NOUVEAU DESIGN SIGNIN - VERSION 2 */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #e0f2f7 0%, #f0f8fa 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .login-container {
            background: white;
            border-radius: 24px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.08);
            max-width: 480px;
            width: 100%;
            padding: 50px 40px;
            position: relative;
            overflow: hidden;
        }

        .login-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(90deg, #20b2aa, #3ecdc7);
        }

        .logo-container {
            text-align: center;
            margin-bottom: 35px;
        }

        .logo {
            max-width: 140px;
            height: auto;
            margin-bottom: 10px;
        }

        .welcome-text {
            text-align: center;
            margin-bottom: 40px;
        }

        .welcome-text h1 {
            color: #2c3e50;
            font-size: 32px;
            font-weight: 700;
            margin-bottom: 8px;
            letter-spacing: -0.5px;
        }

        .welcome-text p {
            color: #95a5a6;
            font-size: 15px;
            font-weight: 400;
        }

        .form-group {
            margin-bottom: 24px;
        }

        .form-label {
            display: block;
            color: #2c3e50;
            font-size: 14px;
            font-weight: 500;
            margin-bottom: 8px;
        }

        .input-wrapper {
            position: relative;
        }

        .input-icon {
            position: absolute;
            left: 18px;
            top: 50%;
            transform: translateY(-50%);
            color: #bdc3c7;
            font-size: 18px;
        }

        .form-input {
            width: 100%;
            padding: 16px 18px 16px 52px;
            border: 2px solid #e8eef1;
            border-radius: 12px;
            font-size: 15px;
            color: #2c3e50;
            transition: all 0.3s ease;
            background: #f8fafb;
            font-family: 'Inter', sans-serif;
        }

        .form-input:focus {
            outline: none;
            border-color: #20b2aa;
            background: white;
            box-shadow: 0 0 0 4px rgba(32, 178, 170, 0.08);
        }

        .form-input::placeholder {
            color: #bdc3c7;
        }

        .form-options {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 28px;
        }

        .remember-me {
            display: flex;
            align-items: center;
            gap: 8px;
            cursor: pointer;
        }

        .remember-me input[type="checkbox"] {
            width: 18px;
            height: 18px;
            border: 2px solid #dfe6e9;
            border-radius: 4px;
            cursor: pointer;
            accent-color: #20b2aa;
        }

        .remember-me label {
            color: #7f8c8d;
            font-size: 14px;
            cursor: pointer;
            user-select: none;
        }

        .forgot-password {
            color: #20b2aa;
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            transition: all 0.2s;
            display: inline-flex;
            align-items: center;
            gap: 5px;
        }

        .forgot-password:hover {
            color: #1a8f89;
            transform: translateX(2px);
        }

        .forgot-password i {
            font-size: 12px;
        }

        .btn-login {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #20b2aa 0%, #3ecdc7 100%);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            box-shadow: 0 4px 15px rgba(32, 178, 170, 0.3);
        }

        .btn-login:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(32, 178, 170, 0.4);
        }

        .btn-login:active {
            transform: translateY(0);
        }

        .signup-link {
            text-align: center;
            margin-top: 28px;
            color: #95a5a6;
            font-size: 14px;
        }

        .signup-link a {
            color: #20b2aa;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.2s;
        }

        .signup-link a:hover {
            color: #1a8f89;
        }

        .alert {
            padding: 14px 18px;
            border-radius: 10px;
            margin-bottom: 24px;
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .alert-danger {
            background: #fff5f5;
            color: #e74c3c;
            border-left: 4px solid #e74c3c;
        }

        .alert-success {
            background: #f0fdf4;
            color: #059669;
            border-left: 4px solid #10b981;
        }

        @media (max-width: 576px) {
            .login-container {
                padding: 40px 28px;
            }

            .welcome-text h1 {
                font-size: 26px;
            }

            .form-input {
                padding: 14px 16px 14px 48px;
            }
            
            .form-options {
                flex-direction: column;
                gap: 12px;
                align-items: flex-start;
            }
        }

        .btn-login.loading {
            opacity: 0.7;
            cursor: not-allowed;
        }

        .btn-login .spinner {
            display: none;
            width: 18px;
            height: 18px;
            border: 2px solid #ffffff;
            border-top-color: transparent;
            border-radius: 50%;
            animation: spin 0.6s linear infinite;
        }

        .btn-login.loading .spinner {
            display: inline-block;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Honeypot: invisible */
        .captcha-honeypot {
            position: absolute !important;
            left: -9999px !important;
            width: 1px !important;
            height: 1px !important;
            opacity: 0 !important;
            pointer-events: none !important;
        }

        /* CAPTCHA Modal */
        .captcha-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .captcha-modal-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(5px);
        }
        .captcha-modal-content {
            position: relative;
            background: white;
            border-radius: 16px;
            box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
            max-width: 440px;
            width: 100%;
            animation: modalSlideIn 0.3s ease-out;
            overflow: hidden;
        }
        @keyframes modalSlideIn {
            from { opacity: 0; transform: translateY(-40px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .captcha-modal-header {
            background: linear-gradient(135deg, #14b8a6 0%, #0d9488 100%);
            color: white;
            padding: 24px;
            text-align: center;
        }
        .captcha-modal-header i { font-size: 32px; display: block; margin-bottom: 10px; }
        .captcha-modal-header h5 { margin: 0; font-size: 18px; font-weight: 600; }
        .captcha-modal-body { padding: 28px; }
        .captcha-modal-body .captcha-question {
            background: linear-gradient(135deg, #0f766e 0%, #14b8a6 100%);
            color: white;
            padding: 18px 20px;
            border-radius: 12px;
            font-size: 15px;
            font-weight: 500;
            text-align: center;
            margin-bottom: 20px;
            line-height: 1.5;
        }
        .captcha-input-group { margin-top: 8px; }
        .captcha-input-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #2c3e50;
            font-size: 14px;
        }
        .captcha-input-group input {
            width: 100%;
            padding: 14px 16px;
            font-size: 18px;
            text-align: center;
            border: 2px solid #e8eef1;
            border-radius: 10px;
            transition: border-color 0.3s, box-shadow 0.3s;
        }
        .captcha-input-group input:focus {
            outline: none;
            border-color: #14b8a6;
            box-shadow: 0 0 0 4px rgba(20, 184, 166, 0.15);
        }
        .captcha-modal-footer {
            padding: 20px 28px;
            border-top: 1px solid #eee;
            display: flex;
            gap: 12px;
            justify-content: flex-end;
        }
        .captcha-modal-footer .btn-modal {
            padding: 12px 24px;
            border-radius: 10px;
            font-weight: 600;
            font-size: 14px;
            cursor: pointer;
            border: none;
            transition: all 0.2s;
        }
        .captcha-modal-footer .btn-modal-secondary {
            background: #f1f5f9;
            color: #475569;
        }
        .captcha-modal-footer .btn-modal-secondary:hover { background: #e2e8f0; }
        .captcha-modal-footer .btn-modal-primary {
            background: linear-gradient(135deg, #14b8a6 0%, #0d9488 100%);
            color: white;
        }
        .captcha-modal-footer .btn-modal-primary:hover { opacity: 0.95; transform: translateY(-1px); }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="logo-container">
            <img src="{{ asset('images/logo.png') }}" alt="Bekri Logo" class="logo" onerror="this.style.display='none'">
        </div>

        <div class="welcome-text">
            <h1>Bon retour</h1>
            <p>Connectez-vous à votre compte</p>
        </div>

        {% if error %}
            <div class="alert alert-danger">
                <i class="fas fa-exclamation-circle"></i>
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}

        {% for message in app.flashes('success') %}
            <div class="alert alert-success">
                <i class="fas fa-check-circle"></i>
                {{ message }}
            </div>
        {% endfor %}

        <form method="post" action="{{ path('app_login') }}" id="loginForm">
            <div class="form-group">
                <label class="form-label" for="email">Adresse email</label>
                <div class="input-wrapper">
                    <i class="fas fa-envelope input-icon"></i>
                    <input 
                        type="email" 
                        class="form-input" 
                        id="email" 
                        name="_username"
                        value="{{ last_username }}"
                        placeholder="nom@exemple.com"
                        required
                        autocomplete="email"
                    >
                </div>
            </div>

            <div class="form-group">
                <label class="form-label" for="password">Mot de passe</label>
                <div class="input-wrapper">
                    <i class="fas fa-lock input-icon"></i>
                    <input 
                        type="password" 
                        class="form-input" 
                        id="password" 
                        name="_password"
                        placeholder="••••••••"
                        required
                        autocomplete="current-password"
                    >
                </div>
            </div>

            <div class="form-options">
                <div class="remember-me">
                    <input 
                        type="checkbox" 
                        id="remember_me" 
                        name="_remember_me"
                    >
                    <label for="remember_me">Se souvenir de moi</label>
                </div>
                <a href="{{ path('forgot_password_request') }}" class="forgot-password">
                    <i class="fas fa-key"></i>
                    <span>Mot de passe oublié ?</span>
                </a>
            </div>

            {# Invisible honeypot + timestamp #}
            <input type="text" name="website" value="" tabindex="-1" autocomplete="off" class="captcha-honeypot" aria-hidden="true">
            <input type="hidden" name="form_timestamp" value="{{ 'now'|date('U') }}">

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button type="submit" class="btn-login" id="loginBtn">
                <i class="fas fa-sign-in-alt"></i>
                <span>Se connecter</span>
                <div class="spinner"></div>
            </button>

            <div class="signup-link">
                Pas encore de compte ? <a href="{{ path('app_register') }}">Créer un compte</a>
            </div>
            <div class="signup-link mt-2">
                <a href="{{ path('request_reactivation') }}">Compte désactivé par un admin ? Demander une réactivation</a>
            </div>
        </form>
    </div>

    <!-- CAPTCHA Modal (shown when login_attempts >= 2) -->
    <div id="captcha-modal" class="captcha-modal" style="display: none;">
        <div class="captcha-modal-overlay" onclick="closeCaptchaModal()"></div>
        <div class="captcha-modal-content">
            <div class="captcha-modal-header">
                <i class="fas fa-shield-alt"></i>
                <h5>Vérification de sécurité</h5>
            </div>
            <div class="captcha-modal-body">
                <div class="captcha-question">
                    <span id="captcha-question-text">{{ captcha_question|default('')|e('js') }}</span>
                </div>
                <div class="captcha-input-group">
                    <label for="captcha-modal-input">Votre réponse</label>
                    <input type="number" id="captcha-modal-input" class="form-input" placeholder="?" min="0" step="1" autocomplete="off">
                </div>
            </div>
            <div class="captcha-modal-footer">
                <button type="button" class="btn-modal btn-modal-secondary" onclick="closeCaptchaModal()">Annuler</button>
                <button type="button" class="btn-modal btn-modal-primary" onclick="submitCaptchaAndLogin()">Vérifier et connecter</button>
            </div>
        </div>
    </div>

    <script>
        var loginAttempts = {{ app.session.get('login_attempts', 0) }};
        var captchaVerified = false;
        var captchaQuestion = {{ captcha_question|default('')|json_encode|raw }};

        function showCaptchaModal(question) {
            document.getElementById('captcha-question-text').textContent = question || captchaQuestion;
            document.getElementById('captcha-modal').style.display = 'flex';
            document.getElementById('captcha-modal-input').value = '';
            setTimeout(function() { document.getElementById('captcha-modal-input').focus(); }, 100);
        }
        function closeCaptchaModal() {
            document.getElementById('captcha-modal').style.display = 'none';
        }
        function submitCaptchaAndLogin() {
            var userAnswer = document.getElementById('captcha-modal-input').value.trim();
            if (!userAnswer) {
                alert('Veuillez entrer une réponse.');
                return;
            }
            var input = document.querySelector('input[name="captcha_answer"]');
            if (!input) {
                input = document.createElement('input');
                input.type = 'hidden';
                input.name = 'captcha_answer';
                document.getElementById('loginForm').appendChild(input);
            }
            input.value = userAnswer;
            captchaVerified = true;
            closeCaptchaModal();
            document.getElementById('loginForm').submit();
        }
        document.addEventListener('DOMContentLoaded', function() {
            var captchaInput = document.getElementById('captcha-modal-input');
            if (captchaInput) {
                captchaInput.addEventListener('keypress', function(e) {
                    if (e.key === 'Enter') { submitCaptchaAndLogin(); }
                });
            }
        });
        document.getElementById('loginForm').addEventListener('submit', function(e) {
            var honeypot = document.querySelector('input[name="website"]');
            if (honeypot && honeypot.value !== '') {
                e.preventDefault();
                return false;
            }
            var ts = parseInt(document.querySelector('input[name="form_timestamp"]').value, 10);
            if (ts && (Math.floor(Date.now() / 1000) - ts) < 3) {
                e.preventDefault();
                alert('Veuillez patienter quelques secondes avant de vous connecter.');
                return false;
            }
            if (loginAttempts >= 2 && !captchaVerified) {
                e.preventDefault();
                showCaptchaModal();
                return false;
            }
            var btn = document.getElementById('loginBtn');
            btn.classList.add('loading');
            btn.querySelector('span').textContent = 'Connexion...';
        });
    </script>
</body>
</html>
